#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        string test;
        cin >> test;
        int size = test.size();
        if(next_permutation(test.begin(),test.end()))
        {
            cout << test << endl;
        }
        else
        {
            cout << "no answer " << endl;
        }
    }
}
