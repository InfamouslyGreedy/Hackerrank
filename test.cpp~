#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <string>
#include <queue>
using namespace std;

class test
{
    public:
        string key;
        test(string key):key(key){}
};
int main()
{
    test now = test("swag");
    test change = now;
    //change.key = "changed";
    cout << " The new " << now.key << " " << change.key<<  endl;
}
